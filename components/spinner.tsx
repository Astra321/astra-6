'use client';

import { Loader } from 'lucide-react';
import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      <Loader />
    </div>
  );
};

export default Spinner;
